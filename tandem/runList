while read amp freq dir
do
	echo 'amp='$amp', freq='$freq', resume from '$dir
	sed 's/AMP/'$amp'/
	     s/FREQ/'$freq'/' runs.f90 > lotus.f90
	time runLotus 16 'caseCamp'$amp'freq'$freq $dir
	wait
done
